VanillaTilt.init(document.querySelector(".box"), {
    max: 15,
    speed: 400,
    glare: true,
    "max-glare": 0.2,
});

VanillaTilt.init(document.querySelector(".box"), {
    max: 10,
    speed: 400,
    glare: true,
    "max-glare": 0.2,
});

VanillaTilt.init(document.querySelector(".box"), {
    max: 10,
    speed: 400,
    glare: true,
    "max-glare": 0.2,
});